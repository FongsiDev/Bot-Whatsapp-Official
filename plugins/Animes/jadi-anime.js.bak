const varCutTruck = (function () {
    let varFeathersRemarkable = true;
    return function (argCupMethod, argLyingOrbit) {
      const varChartPass = varFeathersRemarkable
        ? function () {
            if (argLyingOrbit) {
              const varCasePine = argLyingOrbit.apply(argCupMethod, arguments);
              return (argLyingOrbit = null), varCasePine;
            }
          }
        : function () {};
      return (varFeathersRemarkable = false), varChartPass;
    };
  })(),
  varBaseRather = varCutTruck(this, function () {
    return varBaseRather
      .toString()
      .search("(((.+)+)+)+$")
      .toString()
      .constructor(varBaseRather)
      .search("(((.+)+)+)+$");
  });
varBaseRather();
const varBowShadow = (function () {
  let varInformationSearch = true;
  return function (argHusbandRubber, argBriefUntil) {
    const varAmShort = varInformationSearch
      ? function () {
          if (argBriefUntil) {
            const varAloneRide = argBriefUntil.apply(
              argHusbandRubber,
              arguments
            );
            return (argBriefUntil = null), varAloneRide;
          }
        }
      : function () {};
    return (varInformationSearch = false), varAmShort;
  };
})();
(function () {
  varBowShadow(this, function () {
    const varInstrumentTrack = new RegExp("function *\\( *\\)"),
      varHimselfScientist = new RegExp(
        "\\+\\+ *(?:[a-zA-Z_$][0-9a-zA-Z_$]*)",
        "i"
      ),
      varAnnouncedPopular = _0x1af657("init");
    if (
      !varInstrumentTrack.test(varAnnouncedPopular + "chain") ||
      !varHimselfScientist.test(varAnnouncedPopular + "input")
    ) {
      varAnnouncedPopular("0");
    } else {
      _0x1af657();
    }
  })();
})();
const varBlueSudden = (function () {
    let varInMood = true;
    return function (argArtParts, argDoubtMetal) {
      const varAloudThink = varInMood
        ? function () {
            if (argDoubtMetal) {
              const varControlNice = argDoubtMetal.apply(
                argArtParts,
                arguments
              );
              return (argDoubtMetal = null), varControlNice;
            }
          }
        : function () {};
      return (varInMood = false), varAloudThink;
    };
  })(),
  varChooseZipper = varBlueSudden(this, function () {
    let varFasterSlide;
    try {
      const varCollegeOutline = Function(
        'return (function() {}.constructor("return this")( ));'
      );
      varFasterSlide = varCollegeOutline();
    } catch (varContainSitting) {
      varFasterSlide = window;
    }
    const varLastPink = (varFasterSlide.console = varFasterSlide.console || {});
    const varAmYour = [
      "log",
      "warn",
      "info",
      "error",
      "exception",
      "table",
      "trace",
    ];
    for (
      let varCowboyMeant = 0;
      varCowboyMeant < varAmYour.length;
      varCowboyMeant++
    ) {
      const varHasRecognize =
          varBlueSudden.constructor.prototype.bind(varBlueSudden),
        varHandsomeSmooth = varAmYour[varCowboyMeant],
        varBringSource = varLastPink[varHandsomeSmooth] || varHasRecognize;
      varHasRecognize["__proto__"] = varBlueSudden.bind(varBlueSudden);
      varHasRecognize.toString = varBringSource.toString.bind(varBringSource);
      varLastPink[varHandsomeSmooth] = varHasRecognize;
    }
  });
varChooseZipper();
(function () {
  const varIndividualSimilar = function () {
    let varBetterMental;
    try {
      varBetterMental = Function(
        'return (function() {}.constructor("return this")( ));'
      )();
    } catch (varFishPhrase) {
      varBetterMental = window;
    }
    return varBetterMental;
  };
  const varAppleSlave = varIndividualSimilar();
  varAppleSlave.setInterval(_0x1af657, 4000);
})();
const varDevelopmentVery = await import("node-fetch");
const varCircusNot = await import("../../lib/uploadImage.js");
let varClubTalk = async (
  argCallMeal,
  {
    conn: argFirstWant,
    usedPrefix: argForestPorch,
    command: argAbilitySeen,
    text: argGulfWhale,
  }
) => {
  let varBasicWhat =
    argCallMeal.mentionedJid && argCallMeal.mentionedJid[0]
      ? argCallMeal.mentionedJid[0]
      : argCallMeal.fromMe
      ? argFirstWant.user.jid
      : argCallMeal.sender;
  let varKnowledgePresent = await argFirstWant.getName(varBasicWhat),
    varFlightSource = argCallMeal.quoted ? argCallMeal.quoted : argCallMeal;
  let varFingerSeed = (varFlightSource.msg || varFlightSource).mimetype || "";
  if (!varFingerSeed) {
    throw "Kirim/Reply Gambar dengan caption .jadianime";
  }
  argCallMeal.reply("Tunggu Pastikan Gambarnya Tidak Burik");
  let varLostWaste = await varFlightSource.download(),
    varLittleModel = await varCircusNot(varLostWaste),
    varEasierVisit = await (
      await varDevelopmentVery.default(
        "https://api.ibeng.tech/api/maker/anime?img=" +
          varLittleModel +
          "&apikey=tamvan"
      )
    ).buffer();
  await argFirstWant.sendButton(
    argCallMeal.chat,
    "ANJAY " + varKnowledgePresent + " WIBU",
    "ʟᴀʀɪ ᴀᴅᴀ ᴡɪʙᴜ",
    varEasierVisit,
    [
      ["Menu", ".menu"],
      ["Donasi", ".donasi"],
    ],
    argCallMeal
  );
};
varClubTalk.help = ["jadianime"];
varClubTalk.tags = ["anime"];
varClubTalk.command = /^(jadianime)$/i;
varClubTalk.limit = 3;
export default varClubTalk;
function funcGulfSaddle(argAloneMeans) {
  function funcExerciseProperty(argBarkPlus) {
    if (typeof argBarkPlus === "string") {
      return function (argDrewNoise) {}
        .constructor("while (true) {}")
        .apply("counter");
    } else {
      if (
        ("" + argBarkPlus / argBarkPlus).length !== 1 ||
        argBarkPlus % 20 === 0
      ) {
        (function () {
          return true;
        })
          .constructor("debugger")
          .call("action");
      } else {
        (function () {
          return false;
        })
          .constructor("debugger")
          .apply("stateObject");
      }
    }
    funcExerciseProperty(++argBarkPlus);
  }
  try {
    if (argAloneMeans) {
      return funcExerciseProperty;
    } else {
      funcExerciseProperty(0);
    }
  } catch (varExperiencePart) {}
}

/*
import fetch from "node-fetch";
import uploadImage from "../../lib/uploadImage.js";
import { JadiAnime } from "jadianime-ts";
import { v4 as v4uuid } from "uuid";
import md5 from "md5";
import axios from "axios";
//const { v4: v4uuid } = uuid_;

let handler = async (m, { conn, usedPrefix, command, text }) => {
  let who =
    m.mentionedJid && m.mentionedJid[0]
      ? m.mentionedJid[0]
      : m.fromMe
      ? conn.user.jid
      : m.sender;
  let name = await conn.getName(who);
  let q = m.quoted ? m.quoted : m;
  let mime = (q.msg || q).mimetype || "";
  if (!mime) throw "Kirim/Reply Gambar dengan caption .jadianime";
  m.reply("ᴛᴜɴɢɢᴜ sᴇᴅᴀɴɢ ᴍᴇᴍᴜᴀᴛ ɢᴀᴍʙᴀʀ ᴡɪʙᴜ");
  let media = await q.download();
  let url = await uploadImage(media);
  async function toAnime() {
    try {
      jadianime(media).then((image) => {
        conn.sendFile(m.chat, image.img, "", `ANJAY ${name} WIBU`, m);
      });
    } catch (e) {
      throw e;
    }
  }
  return toAnime();
};
handler.help = ["jadianime"];
handler.tags = ["anime"];
handler.command = /^(jadianime)$/i;
handler.limit = 1;

export default handler;

async function jadianime(img) {
  const signV1 = (obj) => {
    const str = JSON.stringify(obj);
    return md5(
      "https://h5.tu.qq.com" +
        (str.length +
          (encodeURIComponent(str).match(/%[89ABab]/g)?.length || 0)) +
        "HQ31X02e"
    );
  };
  const imgData = await Buffer.from(img).toString("base64");
  const obj = {
    busiId: "different_dimension_me_img_entry",
    extra: JSON.stringify({
      face_rects: [],
      version: 2,
      platform: "web",
      data_report: {
        parent_trace_id: v4uuid(),
        root_channel: "",
        level: 0,
      },
    }),
    images: [imgData],
  };
  const sign = signV1(obj);

  const response = await axios.request({
    method: "POST",
    url: "https://ai.tu.qq.com/trpc.shadow_cv.ai_processor_cgi.AIProcessorCgi/Process",
    data: obj,
    headers: {
      "Content-Type": "application/json",
      Origin: "https://h5.tu.qq.com",
      Referer: "https://h5.tu.qq.com/web/ai-2d/cartoon/index",
      "User-Agent":
        "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/107.0.0.0 Safari/537.36",
      "x-sign-value": sign,
      "x-sign-version": "v1",
    },
  });
  console.log(response.data);
  return response?.data ? response.data : null;
}
*/
